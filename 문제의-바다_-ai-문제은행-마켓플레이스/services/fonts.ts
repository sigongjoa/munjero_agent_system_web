// This file previously contained base64 encoded font data, which was causing errors.
// It is now deprecated and no longer used.
// Font registration is now handled in `services/pdfFonts.ts` by loading
// fonts directly from a reliable CDN via URL.
