
import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="h-[65px] bg-white shadow-sm border-b border-slate-200 flex items-center justify-end px-6">
      {/* Functionality removed as per request */}
    </header>
  );
};

export default Header;